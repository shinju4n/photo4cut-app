import {Layout, Text} from '@ui-kitten/components';
import React from 'react';
const LoginScreen = () => {
  return (
    <Layout>
      <Text>로그인</Text>
    </Layout>
  );
};

export default LoginScreen;
